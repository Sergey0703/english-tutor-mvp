# Гайд по Деплою на Hugging Face Spaces

## ✅ Проверка Готовности

Убедитесь что созданы все файлы:

```
c:\projects\aijarvis\
├── agent.py              ✅ Основной код агента
├── requirements.txt      ✅ Python зависимости
├── Dockerfile           ✅ Docker конфигурация
├── README.md            ✅ Документация + HF metadata
├── .env.example         ✅ Шаблон переменных
├── .gitignore          ✅ Git ignore
├── claude.md           📝 Описание проекта (не для деплоя)
└── DEPLOYMENT_GUIDE.md  📝 Этот файл (не для деплоя)
```

## Шаг 1: Создание Hugging Face Space

1. Зайдите на https://huggingface.co/spaces
2. Нажмите **"Create new Space"**
3. Заполните форму:
   - **Space name**: `english-tutor-mvp` (или любое другое имя)
   - **License**: MIT
   - **SDK**: **Docker** ⚠️ ОБЯЗАТЕЛЬНО выбрать Docker!
   - **Hardware**: Free (2 vCPU, 16GB RAM)
   - **Visibility**: Private (для тестирования)
4. Нажмите **"Create Space"**

## Шаг 2: Загрузка Файлов

### Вариант A: Через Git (Рекомендуется)

```bash
# В терминале/командной строке
cd c:\projects\aijarvis

# Инициализируем Git (если еще не сделано)
git init

# Добавляем все файлы (кроме тех что в .gitignore)
git add .

# Создаем первый коммит
git commit -m "Initial commit: English Tutor MVP"

# Подключаем репозиторий Hugging Face
# Замените YOUR_USERNAME и SPACE_NAME на ваши значения
git remote add space https://huggingface.co/spaces/YOUR_USERNAME/english-tutor-mvp

# Загружаем код
git push space main
```

### Вариант B: Через Web UI

1. Откройте ваш созданный Space
2. Перейдите во вкладку **Files**
3. Нажмите **"Add file" → "Upload files"**
4. Перетащите файлы:
   - `agent.py`
   - `requirements.txt`
   - `Dockerfile`
   - `README.md`
5. Нажмите **"Commit changes to main"**

⚠️ **НЕ загружайте:**
- `.env` (если создали локально)
- `claude.md`
- `DEPLOYMENT_GUIDE.md`
- `.gitignore` (опционально - можно загрузить)
- `.env.example` (опционально - можно загрузить)

## Шаг 3: Настройка Secrets (Переменные Окружения)

1. Откройте ваш Space
2. Перейдите в **Settings** (справа сверху)
3. Прокрутите до раздела **"Repository secrets"**
4. Добавьте 4 переменные (по одной):

### LIVEKIT_URL
- Нажмите **"New secret"**
- Name: `LIVEKIT_URL`
- Value: `wss://first-aaelw7kf.livekit.cloud`
- Нажмите **"Add"**

### LIVEKIT_API_KEY
- Name: `LIVEKIT_API_KEY`
- Value: ``
- Нажмите **"Add"**

### LIVEKIT_API_SECRET
- Name: `LIVEKIT_API_SECRET`
- Value: ``
- Нажмите **"Add"**

### GOOGLE_API_KEY
- Name: `GOOGLE_API_KEY`
- Value: ``
- Нажмите **"Add"**

## Шаг 4: Мониторинг Сборки

1. После добавления всех secrets Space автоматически начнет сборку
2. Перейдите во вкладку **"App"**
3. Нажмите **"View logs"** внизу экрана
4. Наблюдайте за процессом сборки Docker

### Что вы увидите в логах:

```
Building...
Step 1/7 : FROM python:3.11-slim
...
Step 7/7 : CMD ["python", "agent.py", "start"]
Successfully built...
Starting application...
✅ Google API Key найден
✅ EnglishTutorAgent инициализирован
✅ Event handlers configured
```

### Когда сборка завершена:

- Статус Space сменится на 🟢 **"Running"** (зеленый индикатор)
- Это значит что агент запущен и готов к подключению

### Если сборка не удалась (🔴 "Build failed"):

- Проверьте логи ошибок
- Убедитесь что все 4 secrets добавлены
- Проверьте что SDK выбран **Docker**
- Проверьте что файлы загружены корректно

## Шаг 5: Тестирование через LiveKit Playground

1. Откройте https://agents-playground.livekit.io

2. Заполните Connection Settings:
   - **LiveKit URL**: `wss://first-aaelw7kf.livekit.cloud`
   - **API Key**: `APICpeSck5jt2Rm`
   - **API Secret**: `t4jZk0X3wGLvLAwh0d4iigxmrWLkrdEsmwe7FkDVYLT`

3. Нажмите **"Connect"**

4. Разрешите доступ к микрофону в браузере

5. Подождите 2-3 секунды

6. Вы должны услышать:
   ```
   "Hello! I'm ready to help you practice English.
   [читает текст урока про AI]
   What do you think about this topic?"
   ```

7. Попробуйте ответить:
   - "I think AI is very interesting"
   - "Can you repeat the text?"
   - "What is augment?"

### Тестовые Сценарии:

#### Сценарий 1: Базовое Общение
- Вы: "Hello"
- Агент: читает приветствие и текст урока
- Вы: "I think AI will help people"
- Агент: отвечает и продолжает обсуждение

#### Сценарий 2: Вопросы по Тексту
- Вы: "What does the text say about jobs?"
- Агент: объясняет что AI будет дополнять людей, не заменять

#### Сценарий 3: Исправление Грамматики
- Вы: "AI are very good" (ошибка в грамматике)
- Агент: должен исправить "AI is very good"

## Шаг 6: Проверка Логов Агента

Во время тестирования вернитесь во вкладку **"App" → "View logs"** вашего Space.

Вы должны видеть логи в реальном времени:

```
👤 USER: Hello
💬 ASSISTANT: Hello! I'm ready to help...
👤 USER: I think AI is interesting
💬 ASSISTANT: That's a great observation...
```

## Troubleshooting (Решение Проблем)

### Проблема: "Build failed" при сборке

**Решение:**
1. Проверьте что Dockerfile корректен (без изменений)
2. Проверьте что requirements.txt на месте
3. Посмотрите конкретную ошибку в логах
4. Попробуйте Rebuild: Settings → Factory reboot

### Проблема: Agent подключается но не говорит

**Решение:**
1. Проверьте что GOOGLE_API_KEY корректен
2. Проверьте логи на ошибки от Google API
3. Убедитесь что у вас активирован Gemini API в Google Cloud

### Проблема: LiveKit Playground не подключается

**Решение:**
1. Проверьте что LIVEKIT_URL начинается с `wss://`
2. Проверьте что API Key и Secret скопированы БЕЗ пробелов
3. Попробуйте перезагрузить страницу Playground
4. Проверьте что Space в статусе "Running"

### Проблема: Agent не слышит меня

**Решение:**
1. Проверьте что микрофон работает (тестируйте в других приложениях)
2. Разрешите доступ к микрофону в браузере
3. Используйте Chrome/Edge (лучшая поддержка WebRTC)
4. Говорите четко и громко

### Проблема: Высокая задержка (>3 секунды)

**Решение:**
1. Это нормально для бесплатного Hugging Face Space
2. Для production используйте платный Hardware
3. Проверьте ваш интернет (скорость >10 Mbps)

## Следующие Шаги

После успешного тестирования MVP:

1. ✅ **MVP работает** - голосовой канал установлен
2. 📊 **Добавить базу данных** для словаря пользователя
3. 📰 **Интегрировать RSS** для динамического контента
4. 🔄 **Алгоритм подмены слов** в тексте
5. 👥 **Multi-user support** для разных учеников

## Useful Commands

### Локальный тест (без Docker)

```bash
# Создайте .env из .env.example
copy .env.example .env

# Заполните .env вашими ключами

# Установите зависимости
pip install -r requirements.txt

# Запустите агента в dev режиме
python agent.py dev
```

### Git Commands

```bash
# Проверить статус
git status

# Добавить изменения
git add .

# Коммит
git commit -m "Update agent"

# Загрузить на HF Spaces
git push space main
```

### Проверка версий

```bash
# Проверить Python
python --version  # Должно быть 3.11+

# Проверить pip
pip --version
```

## Полезные Ссылки

- LiveKit Docs: https://docs.livekit.io
- Google Gemini API: https://ai.google.dev/gemini-api/docs
- Hugging Face Spaces: https://huggingface.co/docs/hub/spaces
- LiveKit Playground: https://agents-playground.livekit.io

---

🎉 **Поздравляю! Ваш голосовой ИИ-репетитор запущен в облаке!**
